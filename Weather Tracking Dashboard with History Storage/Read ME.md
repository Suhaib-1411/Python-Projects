# Weather Tracking Dashboard

A modular CLI application written in Python that polls real-time weather details via the OpenWeatherMap API, creates timestamped snapshot entries, and calculates trends on saved history.

## Prerequisites

1. Ensure Python 3.x is present on your machine.
2. Install the necessary network dependency:
   ```bash
   pip install requests