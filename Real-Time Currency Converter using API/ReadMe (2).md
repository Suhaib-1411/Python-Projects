# Real-Time Currency Converter

A modular, keyless CLI-based financial utility that queries live currency exchange markets, executes dynamic global value conversions, tracks audit logging history, and prevents unnecessary API resource pooling using internal memory caching layers.

## Core Setup Prerequisites

1. Ensure Python 3.x runtime environments are set up on your machine.
2. Install the necessary network interface extension:
   ```bash
   pip install requests