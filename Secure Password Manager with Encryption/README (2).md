# Secure Password Manager with Encryption

A CLI-based Python application designed to store, retrieve, and query user access credentials securely using Fernet symmetric encryption and SQLite backend storage.

## Prerequisites & Installation

1. Ensure Python 3.8 or higher is installed.
2. Install the necessary cryptography library:
   ```bash
   pip install cryptography